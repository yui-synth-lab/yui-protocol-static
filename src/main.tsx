import React from 'react'
import ReactDOM from 'react-dom/client'
import AppStatic from './ui/AppStatic.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppStatic />
  </React.StrictMode>,
) 